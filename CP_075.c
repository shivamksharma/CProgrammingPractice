/* Write a program to read an integer. Then display the value of that integer in decimal, octal, and hexadecimal notations  */

#include<stdio.h>
int main()
{
	int num = 10;
	printf("\n Decimal Value is %d \t Octal Value is %o \t Hexadecimal Value is %x", num, num, num);
}

